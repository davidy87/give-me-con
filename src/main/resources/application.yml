spring:
  profiles:
    active: dev
    group:
      dev: client,oauth,jwt,gcp,payment
      prod: client,oauth,jwt,aws,gcp,payment

  messages:
    basename: errors

springdoc:
  swagger-ui:
    url: /docs/openapi3.yml
    path: /docs/swagger
    disable-swagger-default-url: true