spring:
  profiles:
    active: test
    include: client,test,API-KEY,oauth,jwt,gcp

  cloud:
    aws:
      s3:
        bucket: test-bucket
      region:
        static: ap-northeast-2
      stack:
        auto: false

  messages:
    basename: errors