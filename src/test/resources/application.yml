spring:
  profiles:
    active: test
    include: client,test,API-KEY,oauth,jwt,gcp

  jpa:
    show_sql: true
    properties:
      hibernate:
        format_sql: true

  cloud:
    aws:
      s3:
        bucket: test-bucket
      region:
        static: ap-northeast-2
      stack:
        auto: false

  messages:
    basename: errors