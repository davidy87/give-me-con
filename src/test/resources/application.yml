spring:
  profiles:
    active: test
    include: API-KEY,oauth,jwt

  cloud:
    aws:
      s3:
        bucket: test-bucket
      region:
        static: ap-northeast-2
      stack:
        auto: false

  messages:
    basename: errors

client:
  base-url: http://localhost:8081
  paths:
    login: /login