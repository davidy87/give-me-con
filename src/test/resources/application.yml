spring:
  profiles:
    active: test
    include: client,API-KEY,oauth,jwt

  cloud:
    aws:
      s3:
        bucket: test-bucket
      region:
        static: ap-northeast-2
      stack:
        auto: false

  messages:
    basename: errors